[project]
name = "dns-cloudflare-mpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mpremote >=1.28.0",
    "esptool>=4.10.0",
]

[tool.distutils.egg_info]
egg_base = "target"