[project]
name = "dns-cloudflare-mpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ampy>=0.1.3.5",
    "esptool>=4.10.0",
]
